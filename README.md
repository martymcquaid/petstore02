# petstore02

This project is managed by Chipify AI Builder.

## Development Server

**IMPORTANT**: The development server is already running and managed by Chipify.

- **Dev Server URL**: `http://127.0.0.1:5199`
- **Preview URL**: `https://api.chitterchat.co.uk/chipify/projects/70bc11b2-c78f-41a3-9d80-7cd38d33cd8d/preview`
- **Port**: `5199`

**DO NOT** start a new dev server. The dev server is automatically started when the project is created and is accessible via the preview URL above.

## Project Structure

- `src/` - Source code
- `src/pages/` - Page components
- `src/components/` - Reusable components
- `package.json` - Dependencies and scripts

## Available Scripts

- `npm run dev` - Start dev server (already running, managed by Chipify)
- `npm run build` - Build for production
- `npm run preview` - Preview production build

## Notes for AI Assistants

When working on this project:
1. **DO NOT** run `npm run dev` - the dev server is already running
2. **DO NOT** start a new dev server process
3. The dev server is accessible at `http://127.0.0.1:5199`
4. Preview the application using the preview URL: `https://api.chitterchat.co.uk/chipify/projects/70bc11b2-c78f-41a3-9d80-7cd38d33cd8d/preview`
5. All file changes will automatically reload in the browser via Vite HMR
